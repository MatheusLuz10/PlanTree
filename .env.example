# Copie este arquivo para ".env" e aponte para a API (Cloudflare Worker).
#
# Em desenvolvimento local, rode o Worker com `npm run dev` dentro de
# worker/ (veja CLOUDFLARE_SETUP.md) e use a URL que ele imprimir
# (normalmente http://127.0.0.1:8787).

VITE_API_BASE_URL=http://127.0.0.1:8787
